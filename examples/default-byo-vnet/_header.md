# Default BYO VNet example

This example deploys the module with the platform landing zone flag set to true while you bring your own existing virtual network. The assumption is that a hub VNet hosting DNS and other shared services has been provided and that the landing zone will attach to that hub VNet for standard network services (DNS, Hybrid Connectivity, Firewalls, etc.).
